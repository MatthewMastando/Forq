import React from 'react';
export const VercelAnalytics = () => {
  // Return null instead of the Analytics component
  // This is a temporary solution until the package is installed
  return null;
};